<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="100dp"
    android:layout_height="130dp"
    android:layout_margin="8dp"
    android:background="@drawable/back_item_main_pastry_categories"
    android:padding="8dp">

    <ImageView
        android:id="@+id/img_category"
        android:layout_width="50dp"
        android:layout_height="60dp"
        android:contentDescription="@string/category_desc"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/img_category" />

    <TextView
        android:id="@+id/txt_category"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:textColor="@color/color_text_black"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="@+id/img_category"
        app:layout_constraintStart_toStartOf="@+id/img_category"
        app:layout_constraintTop_toBottomOf="@+id/img_category"
        tools:text="شیرینی مخصوص" />

</androidx.constraintlayout.widget.ConstraintLayout>